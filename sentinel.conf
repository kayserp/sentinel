# specify path to iqcash.conf or leave blank
# default is the same as IQcashCore
#iqcash_conf=/home/evan82/.iqcashcore/iqcash.conf

# valid options are mainnet, testnet (default=mainnet)
network=mainnet
#network=testnet

# database connection details
db_name=database/sentinel.db
db_driver=sqlite
